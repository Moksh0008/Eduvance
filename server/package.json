{
  "name": "eduvance-server-alias",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix ../client/server",
    "start": "npm run start --prefix ../client/server",
    "test:isolation": "npm run test:isolation --prefix ../client/server"
  }
}
